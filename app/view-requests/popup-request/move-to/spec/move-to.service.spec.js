/* eslint max-statements: 0, angular/file-name: 0, max-nested-callbacks: 0 */
/*import angular  from 'angular';
import module   from '../move-to.module';


describe('Factory: moveToFct', () => {
  let moveToFct;

  beforeEach(angular.mock.module(module));

  beforeEach(inject(_moveToFct_ => {
    moveToFct = _moveToFct_;
  }));


  /*describe('Telescope profile link', () => {
    it('should get link to a profile with spaced names', () => {
      const result = moveToFct.getToTelescope('Foo Bar');

      expect(result).toBe('telescope.com/foo_bar');
    });


    it('should get link to a profile with notspaced names', () => {
      const result = moveToFct.getToTelescope('Foo-Bar');

      expect(result).toBe('telescope.com/foo-bar');
    });
  });


  describe('moveToFct', () => {
    it('should modify and return given non-array parameter', () => {
      const result = moveToFct.moveToFct('foo-bar');

      expect(result).toBe({
        lest: 'foo-bar',
        isList: false,
        isActive: true
      });
    });

    it('should modify and return given array', () => {
      const result = moveToFct.moveToFct([1, 2, 3, 4, 5]);

      expect(result).toBe({
        lest: [1, 2, 3, 4, 5],
        isList: true,
        isActive: true
      });
    });
  });
});
*/
